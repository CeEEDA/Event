<analysis>**original_problem_statement:**
The user is building a comprehensive **Kirmes (Fairground) Billing System**. The current major feature under development is **Tankbeleg-Digitalisierung (Fuel Receipt Digitization)**.

**PRODUCT REQUIREMENTS (Fuel Receipt Digitization):**
1.  **Goal:** Digitize paper fuel receipts from a tanker truck's Epson TM-U295 slip printer.
2.  **Hardware:** A Raspberry Pi with a touchscreen and a USB-to-RS232 adapter will act as a printer emulator, intercepting the serial print data.
3.  **Data Capture & UI:** The Pi will parse the data, and a local frontend will allow the driver to associate it with an order. It must work offline and sync when connected.
4.  **Portal Integration:**
    *   The main web portal must allow for the management of these digitized fuel receipts.
    *   Receipts should be linked to specific orders from the existing Auftragsverwaltung (Order Management).
    *   Admins need to review, edit, approve, and export receipts as PDFs.
    *   GPS data captured by the Pi should be viewable on a map for admins.
    *   The user has provided multiple iterative refinements, including moving all receipt management directly into the order detail view, automatic receipt numbering, PDF layout adjustments, and admin-only bulk editing features.
    *   The system must cache data from the external EpiRent API to improve performance, with a configurable sync interval and a manual trigger.
    *   A manual override for the delivery address is required as the EpiRent API does not reliably provide this information.

**User's preferred language**: Deutsch

**what currently exists?**
A full-stack application (React frontend, FastAPI backend, MongoDB) for the Kirmes Billing System. The Tankbeleg feature has been substantially built out within the main web portal.

*   **Feature Integration:** All fuel receipt management has been integrated directly into the  component, making the standalone  obsolete (the navigation link has been removed).
*   **Core Functionality:** Admins can now view, create, edit, and delete fuel receipts associated with an order. This includes downloading individual receipts or all receipts for an order as a consolidated PDF.
*   **Admin Tools:**
    *   A feature allows admins to adjust the quantity of all receipts in an order by a percentage.
    *   A GPS map modal allows admins to view the location where a receipt was created.
    *   Admin-only controls are correctly implemented using the  hook.
*   **Data & PDF:** The backend data model () and the PDF generation via ReportLab have been updated to match the format of a real receipt image provided by the user, including the company logo.
*   **Automation:** A system automatically assigns sequential IDs (e.g., , ) to manually created receipts and back-fills IDs for older records missing them.
*   **Performance Optimization:** An API caching system for the external EpiRent API is fully implemented. A background task periodically syncs order data into a local MongoDB collection (), and the frontend now loads this cached data for a significant performance boost. This includes a manual Sync button and a configurable interval in the admin settings.
*   **API Workaround:** A manual override for the delivery address has been implemented on the order detail page to work around the EpiRent API's failure to provide this data.

**Last working item**:
-   Last item agent was working: The agent finished implementing a workaround for a limitation in the external EpiRent API. Since the API does not provide the correct delivery address, the agent added a feature allowing the user to **manually edit and save a delivery address override** for each order directly within the . This involved creating a new backend endpoint (), updating the sync logic to respect this override, and building the corresponding UI in the frontend.
-   Status: FINISHED
-   Agent Testing Done: Y
-   Which testing method agent to use? both
-   User Testing Done: N

**All Pending/In progress Issue list**:
-   **Issue 1: (P1) Hardcoded MQTT Broker URL**
    -   **Description:** A recurring technical debt item where the MQTT URL is hardcoded in  instead of being read from an environment variable or the database. This was noted in the initial handoff but has not been addressed.

Issues Detail:
-   **Issue 1: Hardcoded MQTT Broker URL**
    -   **Attempted fixes:** None.
    -   **Next debug checklist:**
        1.  Refactor  to prioritize loading MQTT settings from the database ( collection).
        2.  Fall back to environment variables () if no database config is found.
        3.  Remove the hardcoded  URL.
    -   **Why fix this issue and what will be achieved with the fix?** To allow flexible environment configurations, remove technical debt, and make the system more robust for production.
    -   **Status:** NOT STARTED
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** backend
    -   **Blocked on other issue:** None

**In progress Task List**:
*   None.

**Upcoming and Future Tasks**
-   **Upcoming Tasks:**
    -   **(P0) Fuel Receipt Digitization - Phase 2 (Raspberry Pi Script):** This is the next major task. The user has provided a sample receipt image, which has been analyzed. The work is now unblocked.
        -   **Action:** Develop the Python script for the Raspberry Pi.
        -   The script must listen on the serial port (via a USB-to-RS232 adapter) to intercept data from the Epson printer.
        -   It must parse the incoming ESC/POS printer data to extract receipt details (Menge, Art, Datum, Uhrzeit, etc.) based on the provided sample.
        -   It must capture the Pi's GPS position.
        -   It needs to implement local buffering (e.g., using SQLite) for offline capability.
        -   It needs to sync buffered data to the  endpoint when online.
    -   **(P1) PayPal Integration:** Add PayPal as an alternative payment method.
    -   **(P1) Direct QR Label Printing:** Add a button to send QR codes directly to a printer.
    -   **(P1) Create Windows Installer:** Build the  installer for the Electron desktop app.
-   **Future Tasks:**
    -   **(P1) Self-Hosted MQTT Broker:** Fully migrate to a self-hosted Mosquitto broker.
    -   **(P2) Admin File Size Limits:** Implement configurable file size limits for uploads.

**Completed work in this session**
-   **Tankbeleg Feature - Phase 1 (Portal UI):** Fully implemented the web interface for managing fuel receipts, integrated directly into the .
-   **EpiRent API Caching:** Built a background sync service and caching layer to drastically improve order loading times.
-   **EpiRent API Workaround:** Implemented a manual override for order delivery addresses to compensate for incomplete data from the external API.
-   **Data Model & PDF Overhaul:** Adapted the backend data model and PDF generation to match a real-world fuel receipt provided by the user, including adding the company logo.
-   **Automatic Receipt Numbering:** Implemented logic for auto-incrementing manual receipt numbers and backfilling numbers for older records.
-   **Admin Controls Bug Fix:** Resolved an issue where admin-only buttons were not visible by refactoring the components to use the  hook instead of .
-   **User-Requested UI Refinements:** Removed the standalone fuel receipts page, removed confirm/reject buttons, and adjusted UI layouts based on user feedback.
-   **Password Change:** Fulfilled a user request to change an account password directly in the database.

**Earlier issues found/mentioned but not fixed**
-   **Issue 1: Hardcoded MQTT Broker URL**
    -   **Debug checklist:** The URL in  needs to be replaced with a dynamic configuration loaded from the database or an environment variable.
    -   **Why to solve this issue and what will be achieved with this?** To remove technical debt and allow for flexible environment configurations, especially for the user's local server.
    -   **Should Test frontend/backend/both after fix:** backend
    -   **Is recurring issue?** Y

**Known issue recurrence from previous fork**
-   None from the current session. The previous issue regarding email delivery was mitigated by allowing user SMTP configuration.

**Code Architecture**


**Key Technical Concepts**
-   **API Caching & Background Tasks:** Using  to run a periodic data synchronization task in the background of a FastAPI application. Storing fetched data in a separate MongoDB collection () to serve as a fast-reading cache.
-   **Defensive Programming against External APIs:** Implementing fallbacks (e.g., using contact address when delivery address is null) and manual user overrides when an external API provides incomplete or incorrect data.
-   **Dynamic PDF Generation:** Using the ReportLab library to create multi-page PDFs on the fly, dynamically incorporating data, images (logos), and calculated values.
-   **Component-Driven UI Development:** Consolidating related functionality into a single, rich component () rather than spreading it across multiple pages, based on user workflow feedback.
-   **State Management Hooks:** Correctly using React's  context hook for role-based access control in components, fixing a bug caused by incorrectly trying to read from .

**key DB schema**
-   **fuel_receipts:** 
-   **orders_cache:** (NEW) 
-   **manual_address_overrides:** (NEW) 

**All files of reference**
-   
-   
-   
-   
-   
-   
-    (The sample receipt image that unblocks Phase 2)

**Areas that need refactoring**:
-    to remove the hardcoded MQTT broker URL.
-   The now-unused  could be deleted to clean up the codebase.

**key api endpoints**
-   : (GET) Get all receipts for an order.
-   : (POST) Endpoint for the Raspberry Pi to sync receipts.
-   : (GET) Download a consolidated PDF of all receipts for an order.
-   : (POST) Apply a percentage adjustment to all receipts in an order.
-   : (POST) Manually trigger a sync with the EpiRent API.
-   : (GET) Get the status of the background sync.
-   : (POST) Save a manual address override for an order.
-   : (GET) Get orders (now reads from ).

**Critical Info for New Agent**
-   **You MUST respond in Deutsch.**
-   The next major task is **Phase 2: The Raspberry Pi Script**. This is unblocked. You will need to write a Python script that reads serial data (emulating a printer), parses it based on the provided image, captures GPS, and syncs to the backend.
-   The **EpiRent API is unreliable** regarding the delivery address. The manual override feature is a successful workaround. Do not spend time trying to fix the API fetch again; the issue is with the external provider.
-   All fuel receipt UI is now located within . Do not refer to or modify .

**documents and test reports created in this job**
-   
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **Request:** Check why the delivery address is not showing for an order. (RESOLVED: EpiRent API issue, implemented fallback and manual override)
2.  **Request:** Add a manual location input if the address is not received from the API. (RESOLVED)
3.  **Info:** User provided a screenshot of the correct address in EpiRent UI. (ACKNOWLEDGED)
4.  **Request:** Cache EpiRent API data to reduce load times, with a 30-min sync and manual refresh button. (RESOLVED)
5.  **Request:** Add a GPS map button to each fuel receipt. (RESOLVED)
6.  **Request:** Remove the percentage display from the receipt list. (RESOLVED)
7.  **Bug Report:** A receipt is missing its . (RESOLVED: Implemented backend logic to back-fill missing numbers).
8.  **Bug Report:** Admin buttons are not visible. (RESOLVED: Fixed by using  hook instead of ).
9.  **Request:** Add various admin features: Liter summary, % bulk adjustment, and a Download All button. (RESOLVED)
10. **Request:** Implement logic for auto-numbering manual receipts and use the company logo on the PDF. (RESOLVED)

**Project Health Check:**
-   **Broken:** None.
-   **Mocked:**
    -   The Raspberry Pi printer emulator and data parser are conceptual and need to be built (Phase 2).
    -   The GPS coordinates in the database are currently placeholders; they will come from the Pi.
-   **External Dependency Risk:** The EpiRent API's incompleteness is a known issue. The implemented caching and manual override mitigate this risk.

**3rd Party Integrations**
-   **EpiRent API:** (Active) External ERP system for orders. Caching layer has been built to reduce dependency and improve performance.
-   **Stripe:** (Active) For payments.
-   **OpenStreetMap/ReportLab:** (Active) Used for GPS map display and PDF generation.
-   **paho-mqtt:** (Active) For MQTT communication (contains hardcoded URL).
-   **Electron:** (Exists) For the desktop app wrapper.

**Testing status**
-   **Testing agent used after significant changes:** YES, twice. The reports  and  confirm high pass rates for the fuel receipt feature.
-   **Test files created:** None in this session.
-   **Known regressions:** None.

**Credentials to test flow:**
-   **Administrator (local dev):**  / 
-   **Administrator (user's server):**  /  (Password was updated during this session).

**What agent forgot to execute**
-   The agent did not address the recurring issue of the hardcoded MQTT broker URL in . This remains as technical debt.</analysis>
