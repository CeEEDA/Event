<analysis>**original_problem_statement**: The user is building a comprehensive Kirmes (Fairground) billing and HR management system. PRODUCT REQUIREMENTS: Manage robust telemetry ingestion, order management, disruption logging, team management, HR time-tracking/absences, Kiosk interfaces, and deep hardware integration (DSE 5510, DSE 890, and Sening MultiFlow for Tankbeleg/fuel receipt ingestion). The current goal is to implement an OTA (Over-The-Air) update mechanism for the Tankbeleg Pi devices.

**User's preferred language**: German (WICHTIG: Antworte dem User ausschließlich auf Deutsch! / INSTRUCT NEXT AGENT TO RESPOND IN GERMAN ONLY)

**what currently exists?** 
A mature React/FastAPI/MongoDB app with EpiRent API sync. Kiosk applications for deployment tracking, HR modules, generator telemetry via MQTT/Modbus, and Tankbeleg ingestion via Sening serial interface exist. An OTA-Update panel exists in the Admin area for managing device updates, but the  script currently lacks the client-side mechanism to receive/fetch these updates and restart automatically.

**Last working item**:
    - Last item agent was working: The agent was analyzing the existing OTA logic in the frontend (, ) and the backend script (). The agent then sent an  tool call to propose the implementation plan to the user for building the OTA polling/push mechanism and wiring it to the UI.
    - Status: BLOCKED / USER VERIFICATION PENDING (Waiting on  response)
    - Agent Testing Done: N
    - Which testing method agent to use? backend testing agent / manual testing by python -c and curl (for testing OTA endpoints).
    - User Testing Done: N

**All Pending/In progress Issue list**:  
None strictly identified as bugs/issues.

**In progress Task List**:
 Task 1: OTA Updates for Tankbeleg Pi (P0)
     - Where to resume: First, read the user's response to the pending  call. Next, implement the update polling or push-listener logic in . Ensure it interfaces correctly with the backend endpoints (e.g., ) and that the Admin UI correctly reflects and triggers these updates.
     - What will be achieved with this? Allows the admin to update the  scripts on remote fuel trucks seamlessly from the dashboard without SSHing into each Pi manually.
     - Status: IN PROGRESS
     - Should Test frontend/backend/both after fix? both
     - Blocked on something: User response to the pending  call.

**Upcoming and Future Tasks**
    Upcoming Tasks:
    - (P1) EpiRent Lieferscheine (Delivery Notes) PDF Generation. (Waiting on user input regarding layout/design).
    - (P2) Add Alarm vor Ort geprueft - Sammel-Warning quittieren button to the Generator Diagnose page to resolve alarms and send a DSE-Reset command (Key 35707).
    - (P2) USB-Resilience / Hardware Health Dashboard pro Pi (Show USB-Resets in last 24h, LTE/Eth-Failover-Events, GPS-Fix-Quality).
    
    Future Tasks:
    - (P2) Add GPS Support to Kirmeskiste (Legacy 4-meter variant).
    - (P2) Lastdiagramm Live-Test.
    - (P2) Camera-Foto Upload with EXIF GPS data in Dokumentenablage (Portal side).
    - (P2) Tank-Alarm-Threshold Notification for Generators.

**Completed work in this session**
- Investigated the current OTA payload structure in the backend and localized the frontend OTA UI components (, ). (DONE - Planning Phase)

**Earlier issues found/mentioned but not fixed**
None.

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: None
  - Recurrence count: 0
  - Status: RESOLVED

**Code Architecture**


**Key Technical Concepts**
React, FastAPI, MongoDB, OTA update pipelines, polling vs. MQTT push mechanics, bash scripting, Python file-replacement and  restart logic.

**key DB schema**
  -  / : 

**changes in tech stack**
  - None in this session.

**All files of reference** 
  - 
  - 
  -  (Must add auto-update logic here)
  - 
  - 

**Areas that need refactoring**:
  - N/A

**key api endpoints**
  -  (Identified as the script update payload endpoint)

**Critical Info for New Agent**
- **Language Constraint:** ALWAYS reply in German.
- **Immediate Next Step:** The previous agent paused execution via . Your immediate first step should be to read the user's latest message (which should contain their answer to the plan) and proceed with implementing the OTA mechanics for  based on their feedback.
- **Hardware constraints:** Ensure that  safely replaces its own running file and triggers a systemd restart ( or exiting so systemd restarts it automatically). Be mindful of file permissions and backward compatibility.

**documents and test reports created in this job**
  - None created in this micro-session. 

**Last 10 User Messages and any pending HUMAN messages** 
 1. (User) Shares a screenshot of the existing OTA Updates UI and says: Ich möchte in der Admin Ebene einen Button und dann pushen wir das.
 2. (Agent) Used bash to investigate frontend pages for OTA elements.
 3. (Agent) Used bash to investigate .
 4. (Agent) Proposed plan via  regarding how to implement the OTA feature for the Pi.
 5. *(Pending)* Waiting for human response on the OTA implementation plan. 

**Project Health Check:**
- Broken: None
- Mocked: None

**3rd Party Integrations**
- Stripe (Payments) — requires User API Key
- Open-Meteo (Weather API) — No key required.
- Nominatim (Geocoding API) — No key required.
- EpiRent (Event software API) — Uses user-configured API.

**Testing status**
  - Testing agent used after significant changes: NO
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: None
  - Known regressions: None

**Credentials to test flow:**
- Preview Admin:  / 
- Preview Mitarbeiter:  / 

**What agent forgot to execute**
N/A - Agent correctly paused to gather user confirmation for a major architectural change. 

**Debugging checklist for current issue**
    Issue: Implement OTA for Tankbeleg Pi.
    
    Frontend files to check: 
    -  (Ensure the push button fires the correct API call to the backend for the Tankbeleg Pis).
  
    Backend files to check: 
    -  (Add robust self-updating block that queries , verifies hash, rewrites the script, and restarts).
    -  (Ensure it accurately flags devices as 'update pending' so the Pi knows an update is ready).</analysis>
